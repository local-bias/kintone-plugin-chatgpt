import styled from '@emotion/styled';

export default styled.div`
  position: absolute;
  bottom: 0;
  left: 0;
  width: 100%;
  background: linear-gradient(to bottom, transparent 14%, #fff 55%);
`;
